export interface ThemeStyles {
  logo?: React.CSSProperties;
  logoRuta: string;
  menu: string;
  container: string;
  headerContainer: string;
  headerContent: string;
  button: string;
  link: string;
  linkHome: string;
}
